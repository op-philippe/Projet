{% extends 'base.html.twig' %}

{% block title %}Modifier un lien{% endblock %}

{% block body %}
    <h1>Modifier un lien</h1>

    {{ include('link/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('link_index') }}">Retour à la liste</a>

    {{ include('link/_delete_form.html.twig') }}
{% endblock %}
