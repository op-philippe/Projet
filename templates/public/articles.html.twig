{% if articles is null %}
    <h1>Pas d'articles sur la page</h1>
{% else %}
    {% for article in articles %}
            <article>
                <h3>{{ article.title }}</h3>
                {{ article.content|raw }}
            </article>
    {% endfor %}
{% endif %}

